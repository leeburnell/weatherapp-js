Very simple single page site that fetches data in via the OpenWeatherAPI - pass the fields wanted - name, data, city etc and parse them into the HTML.

Just need to run the html file locally on your machine
